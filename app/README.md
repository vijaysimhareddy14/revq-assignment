# App

React + Vite, no server. Data is pre-loaded from `src/data.json` (generated by ingest).

## Run

```bash
cd app
npm install
npm run dev
# → http://localhost:5173
```

## Routes

- `/` — product list (index, not required but needed to navigate)
- `/product/:id` — product detail page (the required route)
